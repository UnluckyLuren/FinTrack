# Copia este archivo como .env y ajusta los valores
DB_HOST=mysql
DB_NAME=fintrack
DB_USER=fintrack_user
DB_PASSWORD=fintrack_pass_2026

# URL pública del backend (tu túnel de Cloudflare o IP:puerto)
# Ejemplo con Cloudflare Tunnel:
# BACKEND_URL=https://api-fintrack.tudominio.com
# Ejemplo con IP local (solo red local):
# BACKEND_URL=http://192.168.1.X:8080
